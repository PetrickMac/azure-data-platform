// IoT Hub device connectivity over time
// Tracks connected vs total devices for availability monitoring
AzureMetrics
| where ResourceProvider == "MICROSOFT.DEVICES"
| where MetricName in ("connectedDeviceCount", "totalDeviceCount")
| summarize avg(Average) by MetricName, bin(TimeGenerated, 1h)
| order by TimeGenerated desc
